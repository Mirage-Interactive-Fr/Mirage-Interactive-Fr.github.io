import{_ as a,o as t,c as r,a9 as o}from"./chunks/framework.asyjyako.js";const u=JSON.parse('{"title":"Overview","description":"","frontmatter":{},"headers":[],"relativePath":"guide/overview.md","filePath":"guide/overview.md","lastUpdated":null}'),n={name:"guide/overview.md"};function s(i,e,c,h,l,p){return t(),r("div",null,[...e[0]||(e[0]=[o('<h1 id="Overview" tabindex="-1">Overview <a class="header-anchor" href="#Overview" aria-label="Permalink to &quot;Overview {#Overview}&quot;">​</a></h1><p>A test can pass while the code it checks becomes slower. The result is still correct, but a new temporary array, an extra parse or a different algorithm may have increased its cost. PerfChecker helps measure those changes and find where they come from.</p><p>It runs existing Julia TestItems or operations defined in a performance suite. You can compare package releases, Git revisions and Julia runtimes, then open the saved timings and profiles in the terminal, VS Code, a browser or a notebook.</p><h2 id="Start-with-one-operation" tabindex="-1">Start with one operation <a class="header-anchor" href="#Start-with-one-operation" aria-label="Permalink to &quot;Start with one operation {#Start-with-one-operation}&quot;">​</a></h2><p>The manual uses Bibliography, a package for importing and exporting bibliographic entries. We first measure a test that imports an article, exports it and checks that its title and citation key survive. That gives us the cost of the whole test.</p><p>Next, we measure export alone, with the input prepared before timing starts. This lets us compare the same operation across versions. We will read its timings and allocations, profile the calls inside it, and use the comparison in CI.</p><p>Begin with <a href="./installation">installation</a>, then <a href="./first-check">measure the test</a>. The <strong>Next page</strong> links follow this sequence. If you already have a runnable benchmark, start at <a href="./understanding-measurements">understanding the result</a>.</p><h2 id="Adapt-the-example" tabindex="-1">Adapt the example <a class="header-anchor" href="#Adapt-the-example" aria-label="Permalink to &quot;Adapt the example {#Adapt-the-example}&quot;">​</a></h2><p>Replace the Bibliography input and operation with code from your own package. Keep a check of the result: an optimization should still produce the expected answer. When you need several workloads or versions, put those choices in a <a href="./../suites-and-comparisons">suite</a>.</p><h3 id="Direct-@check" tabindex="-1">Direct <code>@check</code> <a class="header-anchor" href="#Direct-@check" aria-label="Permalink to &quot;Direct `@check` {#Direct-@check}&quot;">​</a></h3><p>For an inline experiment, <code>@check</code> accepts a preparation block and an operation to measure. Its <a href="./../reference/api">API reference</a> describes the configuration. <code>PerfConfig</code> validates that configuration before execution; the older <code>Dict</code> form is also supported. The manual uses TestItems and suites because their definitions can also be selected from the graphical interfaces.</p><h2 id="What-runs,-and-where" tabindex="-1">What runs, and where <a class="header-anchor" href="#What-runs,-and-where" aria-label="Permalink to &quot;What runs, and where {#What-runs,-and-where}&quot;">​</a></h2><p>The workload, collector and version are separate choices. For example, <code>export_bibtex</code> remains one operation when you switch from timing it to collecting allocation stacks. You do not need to copy its implementation for each tool.</p><p>PerfChecker&#39;s <strong>controller</strong> prepares the selected versions, schedules work and writes reports. A <strong>worker</strong> is a separate Julia process that loads and measures the target. The interface and plotting code run outside that measurement. Within the worker, the collector determines the boundary: an operation benchmark can exclude input preparation, whereas a TestItem measurement includes its setup and assertions. Startup and compilation can also be measured explicitly. The <a href="/PerfChecker/dev/software-suites#Why-the-worker-has-its-own-environment">suite chapter</a> explains which dependencies belong in each environment.</p><h2 id="Save-a-result-and-reopen-it" tabindex="-1">Save a result and reopen it <a class="header-anchor" href="#Save-a-result-and-reopen-it" aria-label="Permalink to &quot;Save a result and reopen it {#Save-a-result-and-reopen-it}&quot;">​</a></h2><p>A <strong>plan</strong> lists the checks you selected. Executing it creates a <strong>run</strong> whose saved result contains the measurements, source revision, Julia runtime and measurement settings. A plot reads those saved values. You can reopen the same run in another interface without executing the workload again.</p><p>The interfaces handle the report files for you. If you need to read their JSON directly, the <a href="./../reference/run-bundles">format reference</a> explains the fields and format-version identifiers such as <code>schema_version</code>.</p><h2 id="Explore-a-larger-example" tabindex="-1">Explore a larger example <a class="header-anchor" href="#Explore-a-larger-example" aria-label="Permalink to &quot;Explore a larger example {#Explore-a-larger-example}&quot;">​</a></h2><p><a href="./../real-packages/datastructures">DataStructures</a> compares construction and use of 35 containers. <a href="./../real-packages/oxygen">Oxygen</a> compares HTTP handlers and adds network measurements. Both include the scripts, saved results and Pluto notebooks, so you can inspect the figures or run the experiments yourself.</p><p>Use the <strong>Interfaces</strong> section to choose where to work, and <strong>Reference</strong> to look up a function or option. <strong>Further topics</strong> covers native tools, remote workers and comparisons between machines.</p>',20)])])}const m=a(n,[["render",s]]);export{u as __pageData,m as default};
