import{_ as l,C as i,o as d,c,a9 as r,E as s,j as t,a}from"./chunks/framework.asyjyako.js";const w=JSON.parse('{"title":"Understand performance measurements","description":"","frontmatter":{},"headers":[],"relativePath":"guide/understanding-measurements.md","filePath":"guide/understanding-measurements.md","lastUpdated":null}'),h={name:"guide/understanding-measurements.md"};function p(m,e,g,u,f,y){const o=i("DocMedia"),n=i("MeasuredProfiles");return d(),c("div",null,[e[0]||(e[0]=r('<h1 id="Understand-performance-measurements" tabindex="-1">Understand performance measurements <a class="header-anchor" href="#Understand-performance-measurements" aria-label="Permalink to &quot;Understand performance measurements {#Understand-performance-measurements}&quot;">​</a></h1><p>A performance measurement answers a question about a particular operation. For Bibliography, that operation might be exporting one prepared bibliography to BibTeX. Its input size, output and measurement boundary matter as much as the number on the screen. Timing that export answers a different question from timing Julia startup, package loading, input construction and export together.</p><p>Start with a benchmark to see whether the operation became slower or allocated more memory. Then use a profiler to investigate where the cost comes from. Finally, repeat the benchmark after a change and check that the output is still correct. A useful profile identifies a place to investigate; it does not by itself demonstrate an improvement.</p><h2 id="Wall-time:-how-long-the-operation-takes" tabindex="-1">Wall time: how long the operation takes <a class="header-anchor" href="#Wall-time:-how-long-the-operation-takes" aria-label="Permalink to &quot;Wall time: how long the operation takes {#Wall-time:-how-long-the-operation-takes}&quot;">​</a></h2><p><strong>Wall time</strong>, or elapsed time, is the difference between the clock readings at the start and end of an operation. It includes time spent computing and time spent waiting within that boundary. In Bibliography&#39;s <code>export_bibtex</code> benchmark, the prepared input already exists when the clock starts. In an import benchmark, reading the fixture is part of the workload. Always read the workload definition before interpreting the timing.</p><p>Time units are easy to confuse: one second is 1,000 milliseconds (ms), one millisecond is 1,000 microseconds (µs), and one microsecond is 1,000 nanoseconds (ns). A displayed value of 7,000 ns means 7 µs. This is an illustrative conversion, not a performance guarantee for Bibliography. PerfChecker&#39;s plot labels and measurement definitions state the unit; raw collector formats can differ.</p><p>In exported data, <code>By</code> denotes bytes and <code>1</code> denotes a dimensionless quantity, such as a count or fraction. These are unit labels. They differ from the <code>/1</code> at the end of a <a href="./../reference/run-bundles">data-format identifier</a>, which identifies the schema version. A plot may retain an identifier such as <code>julia.wall.time</code> so its measurement can be matched to its definition.</p><p><strong>CPU time</strong> concerns processor execution. It is not interchangeable with wall time: waiting for a file or a lock can increase elapsed time while doing little CPU work. Several threads can also consume CPU time simultaneously. A CPU profile&#39;s sample counts are a third quantity: they describe sampled stacks, not a stopwatch duration for every function call.</p>',8)),s(o,{src:"/examples/bibliography/figures/history-time.svg",alt:"Bibliography export medians across nine tagged versions on a linear microsecond axis",caption:"One hundred samples per tag, Windows, Julia 1.13.0, one worker thread. Inputs match; the dependency stack evolves with the tags. Lower means less elapsed time."}),e[1]||(e[1]=r('<h2 id="Samples,-repetitions-and-distributions" tabindex="-1">Samples, repetitions and distributions <a class="header-anchor" href="#Samples,-repetitions-and-distributions" aria-label="Permalink to &quot;Samples, repetitions and distributions {#Samples,-repetitions-and-distributions}&quot;">​</a></h2><p>A <strong>benchmark sample</strong> records a measured execution, or a batch of executions whose timing is normalized per evaluation. <code>samples</code> requests how many such observations to collect; <code>evals</code> controls evaluations inside a sample. A time budget can limit the actual sample count. The Bibliography history requests 100 samples with one evaluation per sample. Read the exported observations to see what was actually recorded.</p><p>Repeated timings differ because of scheduling, caches, GC and other activity. The <strong>median</strong> is the middle value after sorting the samples. The <strong>minimum</strong> describes the fastest observed sample; the <strong>mean</strong> is sensitive to slow samples. A <strong>percentile</strong>, such as p95, is a threshold containing about 95% of the observed values. A small microbenchmark does not establish a service&#39;s production p95: input diversity, concurrency and load would need their own experiment.</p><p>In the <a href="./../interfaces/visualization">historical gallery</a>, compare the version curve with the sample distribution. A difference between two medians is less convincing when the measurements vary widely. Repeat important comparisons in controlled sessions. A successful measurement run does not imply that a CI regression policy was evaluated.</p>',4)),s(o,{src:"/examples/bibliography/figures/history-samples.svg",alt:"Nine groups containing all 900 export timings, with median markers and visible slow samples",caption:"Each dot is one sample; the dark marks are medians. Horizontal displacement only separates dots. Overlap and slow observations remain visible."}),e[2]||(e[2]=t("h2",{id:"Allocated-bytes,-allocation-count-and-live-memory",tabindex:"-1"},[a("Allocated bytes, allocation count and live memory "),t("a",{class:"header-anchor",href:"#Allocated-bytes,-allocation-count-and-live-memory","aria-label":'Permalink to "Allocated bytes, allocation count and live memory {#Allocated-bytes,-allocation-count-and-live-memory}"'},"​")],-1)),e[3]||(e[3]=t("p",null,[a("An "),t("strong",null,"allocation"),a(" obtains memory for an object. Allocated bytes describe the amount obtained during the operation; allocation count describes how many allocation events occurred. These answer different questions. One large buffer can allocate more bytes than many small objects, while the many small objects may impose more management work. In Bibliography, temporary strings and buffers are useful candidates to investigate when export allocations increase.")],-1)),e[4]||(e[4]=t("p",null,[t("strong",null,"Live memory"),a(" is memory still retained at a point in time. Cumulative allocated bytes are not the same as live bytes or the process's RAM usage. An operation can create and discard many temporary objects while keeping a small live result. Julia allocation measurements also do not account for every native allocator used by a C library. The "),t("a",{href:"./../process-memory"},"memory guide"),a(" explains which process and external-memory counters can complement them.")],-1)),s(o,{src:"/examples/bibliography/figures/history-memory.svg",alt:"Allocated bytes per Bibliography export across nine tags, from 4480 bytes at 0.1.0 to 8352 at 0.4.0",caption:"Julia allocation activity per operation. These values are neither retained heap size nor resident process memory. All nine points come from recorded measurements."}),e[5]||(e[5]=r('<h2 id="Garbage-collection:-reclaiming-unused-objects" tabindex="-1">Garbage collection: reclaiming unused objects <a class="header-anchor" href="#Garbage-collection:-reclaiming-unused-objects" aria-label="Permalink to &quot;Garbage collection: reclaiming unused objects {#Garbage-collection:-reclaiming-unused-objects}&quot;">​</a></h2><p>The <strong>garbage collector</strong>, usually shortened to <strong>GC</strong>, reclaims managed memory that the program no longer needs. This saves the programmer from manually freeing ordinary Julia objects, but collecting memory takes work. Temporary objects can therefore affect performance even when they do not remain alive. See Julia&#39;s <a href="https://docs.julialang.org/en/v1/manual/memory-management/" target="_blank" rel="noreferrer">memory management guide</a> for the runtime&#39;s memory model.</p><p><strong>GC time</strong> is the measured time attributed to collection. A <strong>GC fraction</strong> is that time divided by the sample&#39;s elapsed time; multiply by 100 to express a percentage. These are different units. Zero recorded GC time in a short export does not mean zero allocations: the collector may simply not have needed to run during that sample. Do not add GC time to elapsed time; the measured elapsed interval already contains the collection work that took place in it.</p><h2 id="Flame-graphs:-where-sampled-work-accumulates" tabindex="-1">Flame graphs: where sampled work accumulates <a class="header-anchor" href="#Flame-graphs:-where-sampled-work-accumulates" aria-label="Permalink to &quot;Flame graphs: where sampled work accumulates {#Flame-graphs:-where-sampled-work-accumulates}&quot;">​</a></h2><p>A <strong>call stack</strong> records the chain of calls leading to the current function. A <strong>flame graph</strong> aggregates many such stacks into nested rectangles. Width represents the selected weight, such as CPU samples or allocated bytes. Depth shows the calling relationship. The horizontal position is generally not a timeline: neighbouring rectangles need not represent operations that happened one after the other. See <a href="https://www.brendangregg.com/flamegraphs.html" target="_blank" rel="noreferrer">Brendan Gregg&#39;s flame graph explanation</a>.</p><p>A wide parent includes the weight of its descendants. Do not add parent and child percentages as if they were independent costs. Start with a wide branch, follow it into the relevant Bibliography function, then inspect its source line and full call path. The same function can occur under different callers. PerfChecker&#39;s hover or keyboard-focus details retain that path.</p><p>For a CPU flame graph, width concerns sampled execution. For a wall-time flame graph, it concerns sampled task stacks, including waiting tasks where supported. For an allocation flame graph, it concerns sampled allocation bytes or events. The geometry is similar, but the quantity is different. Colours follow the view&#39;s legend; they are not a universal temperature scale or a verdict that a function is defective.</p><h3 id="Inspect-three-recorded-Bibliography-profiles" tabindex="-1">Inspect three recorded Bibliography profiles <a class="header-anchor" href="#Inspect-three-recorded-Bibliography-profiles" aria-label="Permalink to &quot;Inspect three recorded Bibliography profiles {#Inspect-three-recorded-Bibliography-profiles}&quot;">​</a></h3><p>These graphs come from three completed runs of <code>export_bibtex</code> on the recorded development revision <code>575ec810042cc791fd47a2c025a80246ccf039b5</code>. They explain how to read a profile at one revision. The <a href="./../interfaces/visualization">nine-version gallery</a> answers the separate question of how measured cost changes across releases. All three captures used Windows, Julia 1.13.0 and one worker thread.</p><p>Select a weight below and inspect a wide rectangle. Each view retains at most the 40 highest-weight distinct stacks; its percentages use the retained total. The source filters and this selection can omit other work. The three runs have different collection methods, so their weights must not be added or compared as durations. No correctness oracle or regression threshold was evaluated by these recorded profile runs.</p>',10)),s(n),e[6]||(e[6]=r('<p>In the recorded CPU view, <code>export_bibtex</code> at <code>Bibliography/src/bibtex.jl:211</code> has weight <strong>156 CPU samples out of 159 retained</strong>, or about <strong>98.1%</strong>. That is a share of this filtered capture, not 156 function calls or 156 ns. Its <code>_write_bibtex</code> child at line 162 accounts for 100 of those samples: this is work already included in the parent&#39;s weight. Follow that branch to see the string-writing and buffer-growth paths, then use the allocation view to investigate their memory cost.</p><p>To reproduce these views from your own completed walkthrough runs, pass the CPU, wall-time and allocation report directories in that order:</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">julia</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --startup-file=no</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --project=examples/bibliography/.controller/web</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> examples/bibliography/export-profiles.jl</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> CPU_REPORT</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> WALL_REPORT</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ALLOCATION_REPORT</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> profile-html</span></span></code></pre></div><p><code>CPU_REPORT</code>, <code>WALL_REPORT</code> and <code>ALLOCATION_REPORT</code> are placeholders for the directories printed by the <a href="./../tutorials/bibliography">Bibliography runner</a>. The exporter reads saved evidence; it does not launch a new measurement.</p><h2 id="Warm-code,-first-calls-and-state" tabindex="-1">Warm code, first calls and state <a class="header-anchor" href="#Warm-code,-first-calls-and-state" aria-label="Permalink to &quot;Warm code, first calls and state {#Warm-code,-first-calls-and-state}&quot;">​</a></h2><p>Julia compiles methods as needed. A first call can include compilation that a later call avoids. A <strong>warm</strong> benchmark studies repeated execution after preparation; a <strong>cold</strong> or first-call experiment must explicitly include the appropriate startup, loading or compilation boundary. Improving warm export latency does not establish that <code>using Bibliography</code> became faster.</p><p>Input state also matters. Repeating a mutating operation on the same object may change what later samples do. PerfChecker distinguishes fresh prepared state from explicitly reused state. The Bibliography walkthrough explains why its short CPU and wall-profile operations use verified reusable inputs, while its benchmarks prepare fresh state. Keep the policy visible when comparing results.</p><h2 id="Read-a-result-across-several-tools" tabindex="-1">Read a result across several tools <a class="header-anchor" href="#Read-a-result-across-several-tools" aria-label="Permalink to &quot;Read a result across several tools {#Read-a-result-across-several-tools}&quot;">​</a></h2><table tabindex="0"><thead><tr><th style="text-align:right;">Tool or view</th><th style="text-align:right;">Question it answers</th><th style="text-align:right;">Read first</th><th style="text-align:right;">Do not infer</th></tr></thead><tbody><tr><td style="text-align:right;">BenchmarkTools</td><td style="text-align:right;">How long does this operation take, and what does it allocate?</td><td style="text-align:right;">Time distribution, bytes, allocation count, GC time</td><td style="text-align:right;">Time spent in each internal function</td></tr><tr><td style="text-align:right;">Chairmarks</td><td style="text-align:right;">What repeated cost does this operation have under this sampling configuration?</td><td style="text-align:right;">Sample timing, bytes, allocations, GC fraction</td><td style="text-align:right;">Identical sampling semantics to another collector</td></tr><tr><td style="text-align:right;">CPU profiler</td><td style="text-align:right;">Which call paths appear frequently while profiling execution?</td><td style="text-align:right;">Stack weight and source location</td><td style="text-align:right;">Exact duration or invocation count of every function</td></tr><tr><td style="text-align:right;">Wall-time profiler</td><td style="text-align:right;">Which task stacks occupy the observed interval?</td><td style="text-align:right;">Task states and call paths</td><td style="text-align:right;">That every wide branch consumed CPU</td></tr><tr><td style="text-align:right;">Allocation profiler</td><td style="text-align:right;">Where do sampled Julia allocations originate?</td><td style="text-align:right;">Bytes or events, type, stack, sampling rate</td><td style="text-align:right;">Total native memory or a proven memory leak</td></tr><tr><td style="text-align:right;">Process memory</td><td style="text-align:right;">How does the worker&#39;s memory envelope change?</td><td style="text-align:right;">Counter definition, before/after, scope</td><td style="text-align:right;">That the endpoint delta equals all bytes allocated</td></tr><tr><td style="text-align:right;">Static analyzer</td><td style="text-align:right;">What potential code issue deserves investigation?</td><td style="text-align:right;">Diagnostic, source and analysis assumptions</td><td style="text-align:right;">A measured number of nanoseconds saved</td></tr></tbody></table><p>The <a href="./../reference/checks">check catalog</a> explains these collectors individually. VS Code, Oxygen, Pluto and the REPL present the same saved evidence; changing interface does not change the meaning of the measurement.</p><h2 id="Try-it-with-Bibliography" tabindex="-1">Try it with Bibliography <a class="header-anchor" href="#Try-it-with-Bibliography" aria-label="Permalink to &quot;Try it with Bibliography {#Try-it-with-Bibliography}&quot;">​</a></h2><p>Open <a href="./../interfaces/visualization">the version gallery</a> and choose <strong>Export · time</strong>. Inspect a point and read its version and unit. Switch to <strong>Export · allocated bytes</strong> to ask whether the memory cost follows the same pattern. Switch to <strong>Export · sample distribution</strong> to examine variation hidden by the summary curve, then <strong>Export · change from 0.1.0</strong> for the relative difference.</p><p>Before calling a change a regression, check the source and dependency revisions, available workloads, input contract and correctness result. A negative relative change means a decrease for these cost metrics. It is not automatically a pass: the comparison still needs a declared acceptance policy. Follow the <a href="./../tutorials/bibliography">complete walkthrough</a> to reproduce the measurements and the fixed-dependency comparison that investigates a specific export change.</p>',13))])}const v=l(h,[["render",p]]);export{w as __pageData,v as default};
