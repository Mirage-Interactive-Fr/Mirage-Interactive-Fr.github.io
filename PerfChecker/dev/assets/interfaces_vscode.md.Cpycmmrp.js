import{_ as o,C as i,o as r,c as s,a9 as t,E as n}from"./chunks/framework.asyjyako.js";const f=JSON.parse('{"title":"VS Code","description":"","frontmatter":{},"headers":[],"relativePath":"interfaces/vscode.md","filePath":"interfaces/vscode.md","lastUpdated":null}'),l={name:"interfaces/vscode.md"};function c(d,e,p,h,u,m){const a=i("DocMedia");return r(),s("div",null,[e[0]||(e[0]=t('<h1 id="VS-Code" tabindex="-1">VS Code <a class="header-anchor" href="#VS-Code" aria-label="Permalink to &quot;VS Code {#VS-Code}&quot;">​</a></h1><p>The PerfChecker extension adds performance items to VS Code&#39;s Testing view. You can run one item, inspect its measurements, or select a suite and compare package versions without leaving the editor.</p><h2 id="Prepare-the-workspace" tabindex="-1">Prepare the workspace <a class="header-anchor" href="#Prepare-the-workspace" aria-label="Permalink to &quot;Prepare the workspace {#Prepare-the-workspace}&quot;">​</a></h2><p>Install the PerfChecker editor extension and use a Julia environment containing PerfChecker V1, TestItemRunner and your package&#39;s test dependencies. During the unpublished V1 preview, install the supplied <code>.vsix</code> through <strong>Extensions → Install from VSIX…</strong>, then reload the window. The extension source is in the <a href="https://github.com/Mirage-Interactive-Fr/PerfCheckerVSCode" target="_blank" rel="noreferrer">PerfCheckerVSCode repository</a>.</p><p>For the recorded example, first run <code>setup.jl items</code> from <code>examples/bibliography/</code> in the PerfChecker checkout. Open that directory in VS Code, and set <strong>PerfChecker: Runner Project</strong> (<code>perfchecker.runnerProject</code>) to the absolute path of its <code>.controller/items</code> directory.</p><h2 id="Run-one-existing-item" tabindex="-1">Run one existing item <a class="header-anchor" href="#Run-one-existing-item" aria-label="Permalink to &quot;Run one existing item {#Run-one-existing-item}&quot;">​</a></h2><ol><li><p>Open the command palette and choose <strong>PerfChecker: Discover existing test items</strong>.</p></li><li><p>In Testing, expand <strong>PerfChecker items</strong> and select <strong>Bibliography export workload</strong>.</p></li><li><p>Run that item. Leave <strong>Bibliography format API</strong> unselected for this first run.</p></li><li><p>Inspect the outcome and measurements. The screenshot below shows this selection.</p></li></ol><p>For your own package, open its root and select its prepared test environment in the same setting. You can use the <a href="./../guide/first-check">downloadable item</a> instead of the pinned Bibliography example.</p><p>To measure existing <code>@testitem</code> declarations directly, use <strong>PerfChecker: Discover existing test items</strong> and the <strong>PerfChecker items</strong> controller in the Testing view. Select an individual item or a subset. See <a href="./../test-items">existing test items</a> for environment setup, tags and the distinction between correctness and budgets.</p>',9)),n(a,{src:"/examples/bibliography/vscode/native-items.png",alt:"Bibliography export workload passed in VS Code while its format API item remains unrun",caption:"One selected Bibliography item executed through PerfChecker's native Testing controller. Follow the Bibliography walkthrough to prepare the same workspace."}),e[1]||(e[1]=t('<p>The extension reads the same suite definitions and saved results as the Julia API, Web interface (Oxygen) and Pluto.</p><h2 id="Read-the-result-before-comparing-it" tabindex="-1">Read the result before comparing it <a class="header-anchor" href="#Read-the-result-before-comparing-it" aria-label="Permalink to &quot;Read the result before comparing it {#Read-the-result-before-comparing-it}&quot;">​</a></h2><p>The Testing view shows whether the selected item completed successfully. Its overall run duration can include starting the worker and arranging the run. The item measurement has its own boundary, including the code and assertions inside that item. A prepared <code>export_bibtex</code> benchmark excludes preparation outside the timed operation, so these durations answer different questions.</p><p>In a result panel, read the collector, unit and scope alongside the value. Allocated bytes describe allocation activity, while a flame graph attributes sampled work to call paths. A green functional test does not establish a performance budget. The <a href="./../guide/understanding-measurements">measurement tutorial</a> explains these distinctions with recorded Bibliography results.</p><h2 id="Workspace-model" tabindex="-1">Workspace model <a class="header-anchor" href="#Workspace-model" aria-label="Permalink to &quot;Workspace model {#Workspace-model}&quot;">​</a></h2><p>The Explorer and Testing views present:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>package → business feature → check type → target</span></span></code></pre></div><p>From any relevant node you can run the selection, open its visual output, view worker logs, or jump to the exact workload script. Check types remain selectable options of a business feature, so <code>import_bibtex</code> is not duplicated into fake features such as <code>import_bibtex_profile</code>.</p><h2 id="Visual-suite-editor" tabindex="-1">Visual suite editor <a class="header-anchor" href="#Visual-suite-editor" aria-label="Permalink to &quot;Visual suite editor {#Visual-suite-editor}&quot;">​</a></h2><p>This part requires a software suite file. For Bibliography, prepare with <code>setup.jl</code>, select <code>.controller/core</code> as the runner project and choose <code>suite.jl</code>. The native-item environment above and the suite controller have different jobs. Open <strong>PerfChecker: Open visual suite editor</strong> from the command palette. The editor supports:</p><ul><li><p>global and per-feature check-type selection;</p></li><li><p>version ranges, search, sorting, colour labels, and drag-and-drop ordering;</p></li><li><p>named comparison targets for a branch, tag, commit, working tree, or release;</p></li><li><p>exact or grouped baselines with median, mean, minimum, or maximum aggregation;</p></li><li><p>direct execution and progress updates;</p></li><li><p>persistent selection and documentation blocks in <code>perf/perfchecker-ui.json</code>, a reusable JSON configuration file.</p></li></ul><p>The comparison-target picker scans the selected package repository for local and remote branches, tags, and recent commits. It also parses a plain ref, a full SHA, GitHub/GitLab tree/tag/commit URLs, and <code>owner/repository@ref</code> shorthand. Set a compatibility version when an older or experimental target needs a specific workload variant.</p><h2 id="Results" tabindex="-1">Results <a class="header-anchor" href="#Results" aria-label="Permalink to &quot;Results {#Results}&quot;">​</a></h2><p>The output action discovers the latest result matching the selected suite, package, feature, check, or target. Available views include BenchmarkTools and Chairmarks distributions, version trajectories, deltas, allocation pie/file/ line/heatmap views, and allocation/CPU/wall-time flame graphs.</p><p>Standalone HTML views provide point inspection for timing distributions and version series, and hover or keyboard focus for SVG flame cells. Other Makie inspector callbacks require a live Julia session. Flame tooltips expose the full call path and source location; colour semantics identify allocation, garbage collection, runtime dispatch, and unstable inference evidence when the collector supplied it.</p><p>Use the <a href="./../tutorials/bibliography">Bibliography walkthrough</a> to try a complete suite and two native items that reuse its upstream tests and export workload.</p><h2 id="Optional-investigations" tabindex="-1">Optional investigations <a class="header-anchor" href="#Optional-investigations" aria-label="Permalink to &quot;Optional investigations {#Optional-investigations}&quot;">​</a></h2><p>For the shared-scenario workflow, <strong>PerfChecker: Open investigations</strong> adds discovery from tests, explicit adoption, measurements, source-linked diagnostics, saved advice and before/after comparison. Julia CodeLens and the Testing view select the same declared scenarios. See <a href="./../shared-scenarios">Shared scenarios</a> for the lifecycle contract, prepared environments and qualification limits.</p><p>The panel also exposes the searchable tool catalogue, CI synchronization, filterable CPU/allocation stacks, bounded investigations and explanations from an optional configured model. <strong>Model settings</strong> selects an endpoint/protocol or an advisor JSON file; <strong>Explain with configured model</strong> consumes saved advice. <code>advisorInvestigates</code> remains disabled by default. See <a href="./../advisors">Optional models</a> for provider settings and evidence boundaries.</p>',19))])}const b=o(l,[["render",c]]);export{f as __pageData,b as default};
