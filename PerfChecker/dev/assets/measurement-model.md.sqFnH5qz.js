import{_ as t,o as a,c as i,a9 as r}from"./chunks/framework.asyjyako.js";const h=JSON.parse('{"title":"Measurement model","description":"","frontmatter":{},"headers":[],"relativePath":"measurement-model.md","filePath":"measurement-model.md","lastUpdated":null}'),s={name:"measurement-model.md"};function n(o,e,l,d,c,p){return a(),i("div",null,[...e[0]||(e[0]=[r(`<h1 id="Measurement-model" tabindex="-1">Measurement model <a class="header-anchor" href="#Measurement-model" aria-label="Permalink to &quot;Measurement model {#Measurement-model}&quot;">​</a></h1><p>This reference defines the scope and comparability rules behind saved metrics. For an introduction with plots, start with <a href="./guide/understanding-measurements">Understand performance measurements</a>. Use this page when defining a collector or deciding whether two records describe the same measurement.</p><p>PerfChecker models performance as a product of dimensions, not as one generic time or memory number:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>resource × lifecycle phase × execution regime × scope × method × statistic</span></span></code></pre></div><p>For example, startup, import, compilation, first execution, and steady-state time are different measurements. Julia heap bytes, native heap bytes, process RSS, and GPU memory are also different resources. A report may display them together, but it may not silently add or compare them.</p><h2 id="Required-measurement-context" tabindex="-1">Required measurement context <a class="header-anchor" href="#Required-measurement-context" aria-label="Permalink to &quot;Required measurement context {#Required-measurement-context}&quot;">​</a></h2><p>Every measurement definition should state:</p><ul><li><p>metric ID, canonical unit, collector and definition version;</p></li><li><p>phase: resolve, install, precompile, startup, import, first execution, steady state, endurance, or shutdown;</p></li><li><p>regime: clean depot, cached depot/fresh process, warm process, or steady state;</p></li><li><p>scope: expression, method, feature, task, process, process tree, interface, service, database, or device;</p></li><li><p>clock or counter source and whether collection is exact, repeated, sampled, static, or estimated;</p></li><li><p>sample and aggregation semantics;</p></li><li><p>whether compilation, GC, child processes, GPU synchronization, loopback, and protocol overhead are included;</p></li><li><p>expected perturbation, required privileges, and incompatible collectors;</p></li><li><p>runtime, source, dependency, artifact, platform, hardware, thread, cache, and environment provenance;</p></li><li><p>a semantic outcome proving that both candidates performed equivalent work.</p></li></ul><p>Values are numerically comparable only when their versioned measurement definitions and comparison keys permit it.</p><p>Suite bundles now retain controller thread count, hardware fingerprint, local Git revision/branch/dirty state, and SHA-256 fingerprints of the effective Project and Manifest. Different OS, architecture, Julia thread count or known hardware fingerprints make a comparison <code>incomparable</code>. A changed Julia runtime or resolved environment remains visible as a warning because controlled runtime campaigns may intentionally vary one of those axes. Missing legacy fingerprints never silently become proof that two machines are identical.</p><h2 id="Julia-performance-families" tabindex="-1">Julia performance families <a class="header-anchor" href="#Julia-performance-families" aria-label="Permalink to &quot;Julia performance families {#Julia-performance-families}&quot;">​</a></h2><table tabindex="0"><thead><tr><th style="text-align:right;">Family</th><th style="text-align:right;">Evidence kept distinct</th><th style="text-align:right;">Typical collector or adapter</th></tr></thead><tbody><tr><td style="text-align:right;">Warm CPU execution</td><td style="text-align:right;">latency distribution, throughput, CPU user/system, GC</td><td style="text-align:right;">BenchmarkTools, Chairmarks, Julia timers</td></tr><tr><td style="text-align:right;">Startup and import</td><td style="text-align:right;">bare startup, project activation, <code>using</code>, <code>__init__</code>, extension load</td><td style="text-align:right;">fresh processes, <code>@time_imports</code></td></tr><tr><td style="text-align:right;">Compilation and TTFX</td><td style="text-align:right;">first execution, inference, code generation, recompilation, dynamic dispatch</td><td style="text-align:right;">trace flags, SnoopCompileCore, JET</td></tr><tr><td style="text-align:right;">Precompile and images</td><td style="text-align:right;"><code>Pkg.precompile</code>, cache hit/miss, <code>.ji</code>/native image size, sysimage and app size</td><td style="text-align:right;">PrecompileTools, PkgCacheInspector, PackageCompiler</td></tr><tr><td style="text-align:right;">Inference and invalidation</td><td style="text-align:right;">inference time, unstable returns, dispatch sites, invalidation trees and consequential recompilation</td><td style="text-align:right;">JET, SnoopCompileCore, Cthulhu links</td></tr><tr><td style="text-align:right;">CPU profiles</td><td style="text-align:right;">self/cumulative samples, task/thread, Julia and native frames</td><td style="text-align:right;"><code>Profile</code>, PProf, Speedscope</td></tr><tr><td style="text-align:right;">Scheduling and waiting</td><td style="text-align:right;">runnable/running/waiting tasks, locks/channels, fairness and saturation</td><td style="text-align:right;">wall-time profile, task metrics when supported</td></tr><tr><td style="text-align:right;">Julia allocation and GC</td><td style="text-align:right;">bytes/count per operation, allocation sites/types/stacks, pauses and sweeps</td><td style="text-align:right;"><code>Profile.Allocs</code>, <code>@allocated</code>, AllocCheck diagnostics</td></tr><tr><td style="text-align:right;">Heap and process memory</td><td style="text-align:right;">Julia heap, retained objects, RSS/commit peak, page faults, native heap</td><td style="text-align:right;">heap snapshots, process sampler, platform profilers</td></tr><tr><td style="text-align:right;">Microarchitecture</td><td style="text-align:right;">cycles, instructions, IPC, cache/TLB/branch misses, migrations, bandwidth/NUMA</td><td style="text-align:right;">LinuxPerf, LIKWID, ThreadPinning</td></tr><tr><td style="text-align:right;">File and storage I/O</td><td style="text-align:right;">logical/physical bytes, operations, latency, warm/cold cache and compression</td><td style="text-align:right;">application counters plus OS/process adapters</td></tr><tr><td style="text-align:right;">Network and services</td><td style="text-align:right;">payload/interface/wire bytes and packets, connections, retries, retransmissions, client/server latency</td><td style="text-align:right;">explicit workload counters, isolated interfaces, ETW/eBPF providers</td></tr><tr><td style="text-align:right;">GPU/accelerator</td><td style="text-align:right;">device compile, launch, kernel, transfer, synchronization, occupancy and VRAM</td><td style="text-align:right;">CUDA/AMDGPU and vendor profilers</td></tr><tr><td style="text-align:right;">Parallel/distributed</td><td style="text-align:right;">speedup, efficiency, strong/weak scaling, worker startup, serialization and imbalance</td><td style="text-align:right;">thread/process matrices, MPI/vendor tools</td></tr><tr><td style="text-align:right;">Energy</td><td style="text-align:right;">joules/run, joules/op, power and energy-delay product</td><td style="text-align:right;">RAPL/LIKWID and vendor tools</td></tr><tr><td style="text-align:right;">Domain algorithm</td><td style="text-align:right;">growth with size, time-to-solution, accuracy/residual, iterations/evaluations</td><td style="text-align:right;">workload-declared outcomes</td></tr><tr><td style="text-align:right;">Load/endurance</td><td style="text-align:right;">tail latency, backpressure, errors, recovery and memory growth</td><td style="text-align:right;">isolated staged-load scenarios</td></tr></tbody></table><h2 id="Process-and-external-memory" tabindex="-1">Process and external memory <a class="header-anchor" href="#Process-and-external-memory" aria-label="Permalink to &quot;Process and external memory {#Process-and-external-memory}&quot;">​</a></h2><p>Setting <code>:process_resources =&gt; true</code> on any feature now records an orthogonal resource envelope around that feature&#39;s backend collection. On Windows the collector uses PSAPI working-set, lifetime peak and private commit counters. On Linux it reads <code>VmRSS</code> and <code>VmHWM</code> from <code>/proc/&lt;pid&gt;/status</code>, plus private mappings from <code>smaps_rollup</code> when permitted. Unsupported fields are absent; they are never reported as zero.</p><p>This envelope does not replace backend samples. In particular, BenchmarkTools&#39; <code>memory</code> and <code>allocs</code> columns remain Julia allocation evidence, while process RSS and private bytes describe the whole isolated worker. The OS high-water mark is a process-lifetime value. <code>process.rss.new_lifetime_peak/v1</code> is only the growth of that high-water mark during the collection, not an independently reset peak for each workload invocation.</p><p>A feature that owns native memory may additionally expose a zero-argument callback named by <code>:external_memory_probe</code>. The callback implements <code>perfchecker-external-memory/1</code> and reports current live and reserved bytes plus optional cumulative allocated/freed counters. See <a href="./process-memory">Process and external memory</a> for the exact grammar and failure semantics.</p><p>Static analyzers emit diagnostics, not timings. An AllocCheck or JET finding can explain risk or guide attribution, but it is never presented as measured cost.</p><h2 id="Package-archetypes" tabindex="-1">Package archetypes <a class="header-anchor" href="#Package-archetypes" aria-label="Permalink to &quot;Package archetypes {#Package-archetypes}&quot;">​</a></h2><p>A package or feature can select several archetypes:</p><ul><li><p><code>pure_compute</code>, <code>stateful_mutation</code>, <code>compiler_heavy</code>;</p></li><li><p><code>data_io</code>, <code>async_service</code>, <code>native_ffi</code>;</p></li><li><p><code>accelerator</code>, <code>distributed</code>, <code>solver_stochastic</code>;</p></li><li><p><code>interactive_rendering</code> and <code>load_endurance</code>.</p></li></ul><p>Archetypes select sensible presets but never remove the ability to configure individual collectors. Stateful and externally visible workloads must define setup/teardown and semantic outcomes per sample.</p><h2 id="Network-accounting" tabindex="-1">Network accounting <a class="header-anchor" href="#Network-accounting" aria-label="Permalink to &quot;Network accounting {#Network-accounting}&quot;">​</a></h2><p>Network counters describe different attribution scopes. PerfChecker distinguishes:</p><ol><li><p><code>application</code>: payload bytes, packets/frames, operations and connections explicitly reported by the workload;</p></li><li><p><code>host_interface</code>: operating-system interface byte and packet deltas; useful immediately, but contaminated by unrelated traffic;</p></li><li><p><code>isolated_interface</code>: the same counters inside a network namespace, container, VM, or otherwise dedicated interface; suitable for budgets;</p></li><li><p><code>process_tree</code>: packets/bytes attributed by a platform facility such as ETW or eBPF to the worker closure;</p></li><li><p><code>wire</code>: packet capture including transport overhead, opt-in because packet metadata can be sensitive.</p></li></ol><p>The built-in <code>:network</code> backend covers level 1. The <code>:network_interface</code> backend and <code>measure_network_interface</code> cover level 2 on Windows and Linux. They report bytes, packets, discards, interface, capture layer and attribution scope. The <code>:network_isolated</code> backend and <code>measure_isolated_network_command</code> cover level 3 on native Linux or WSL2 with a loopback-only namespace or outbound IPv4 through <code>slirp4netns</code>. Package-level CI gates require level 3 or 4, or an otherwise controlled interface; host-interface evidence remains informative.</p><p>Latency is always labeled with its context: in-process, loopback, client end-to-end, server, DNS/TLS, or scheduler wait. Remote latency can still be compared in controlled experiments, but is not silently attributed to package code.</p><h2 id="Native-dependency-closure" tabindex="-1">Native dependency closure <a class="header-anchor" href="#Native-dependency-closure" aria-label="Permalink to &quot;Native dependency closure {#Native-dependency-closure}&quot;">​</a></h2><p>For JLLs, DLLs, C/C++/Fortran/Rust wrappers, embedded databases and services, PerfChecker follows four layers:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>declared package → resolved artifact → loaded native image</span></span>
<span class="line"><span>                 → spawned process / contacted service</span></span></code></pre></div><p><code>dependency_evidence()</code> implements the first current-process snapshot: resolved Pkg packages, reachable <code>Artifacts.toml</code> files and loaded shared libraries. <code>ProbeSpec</code> complements that inventory with a feature-owned functional check inside the exact prepared target worker. A probe should force the smallest safe operation that proves the ABI, required symbol, device or service is usable; successful resolution alone never makes that claim. The inventory still does not cover native heap allocations or arbitrary child processes.</p><p>Installation/download/extraction, JLL import, loader activity, FFI first call, FFI steady-state overhead and native execution are separate phases. A fully attributed native comparison would also need to fingerprint the effective artifact overrides, binary digests/build IDs, ABI, symbols, allocator, compiler/linker flags, service version/configuration and dataset. Julia allocation counters never claim to cover native allocators.</p><p>Release and sanitizer/Valgrind/ETW deep-diagnostic lanes are separate because instrumentation changes performance. Raw native profiles stay artifacts; normalized frames retain image/build identity and symbolization status.</p><h2 id="Julia-stable-versus-candidate-runtimes" tabindex="-1">Julia stable versus candidate runtimes <a class="header-anchor" href="#Julia-stable-versus-candidate-runtimes" aria-label="Permalink to &quot;Julia stable versus candidate runtimes {#Julia-stable-versus-candidate-runtimes}&quot;">​</a></h2><p><code>JuliaRuntimeSpec</code> introduces a runtime axis separate from package versions. Moving Juliaup selectors such as <code>release</code>, <code>rc</code>, and <code>nightly</code> are probed in a fresh process and the exact version, commit, bindir and LLVM version are frozen in the run plan before workers start.</p><p><code>julia_runtime_suite_command</code> builds the isolated child-controller command for one runtime. PerfChecker loads before the measurement worker starts; its own compilation is not included in the feature measurement. This avoids relying on Julia serialization compatibility between a controller and a Malt worker from different runtime generations.</p><p><code>run_julia_runtime_campaign</code> executes the same suite under an explicit baseline and one or more candidate Julia runtimes, retains the exact version/commit/LLVM probe for each run, and compares the emitted bundles without treating a runtime version difference as an automatic incompatibility. The CLI form is:</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">julia</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --startup-file=no</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --project=.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -e</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;using PerfChecker; exit(perfchecker_main(ARGS))&#39;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> julia-campaign</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --suite=perf/suite.jl</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --baseline=release</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --candidate=rc</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --candidate=nightly</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --reports=perf/julia-runtimes</span></span></code></pre></div><p>Optional repeated <code>--limit=metric=fraction</code> arguments turn diagnostic deltas into CI gates. The campaign JSON keeps runtime identity and comparison evidence; stdout/stderr remain excluded from the default serialized report.</p><p>Repeated <code>--statistic=metric=statistic</code> arguments apply the same median, mean, minimum, maximum, p95, or p99 policy to cross-runtime comparisons. This keeps a candidate runtime from passing merely because its median is stable while its tail latency or peak-memory distribution regresses.</p><p>Failures are phase-classified. Resolver incompatibilities remain distinct from precompile/load ecosystem incompatibilities (including a <code>ReadOnlyMemoryError</code> while loading an AllocCheck/GPUCompiler chain), runtime crashes, suite failures, launch failures and missing reports. This prevents a candidate Julia runtime from being blamed for a performance regression when no comparable workload was measured.</p><p>Two campaigns answer different questions:</p><ul><li><p>strict runtime attribution keeps source, dependency graph, artifacts, workload, machine and settings fixed while Julia changes;</p></li><li><p>realistic compatibility resolves independently under each Julia runtime and reports dependency differences, without attributing their effects to Julia.</p></li></ul><p>Triage proceeds from a confirmed paired regression to CPU/wall/allocation/GC and compiler evidence, profile differencing, smallest-feature reruns, bounded bisection where builds exist, and finally either a reproduced MWE or an investigation bundle that preserves uncertainty.</p>`,43)])])}const u=t(s,[["render",n]]);export{h as __pageData,u as default};
